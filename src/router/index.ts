import App from '../App'

const routes: any = {
  path: '/',
  component: App,
  childRoutes: [
    // { path: 'about', component: About },
    // { path: 'inbox', component: Inbox }
  ]
}
export default routes
